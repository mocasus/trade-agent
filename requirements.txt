# Core dependencies (required)
ccxt>=4.0
pyyaml>=6.0
openai>=1.0
httpx>=0.24
apscheduler>=3.10

# Indicators (optional — falls back to manual calc if not installed)
# pandas_ta>=0.3  # not available for Python 3.11 — using manual fallback

# Telegram notifier (only if using telegram plugin)
python-telegram-bot>=20.0
